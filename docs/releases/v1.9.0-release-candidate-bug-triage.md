# v1.9.0 Release-Candidate Bug Triage

## Scope

This triage reviewed the current v1.9.0 release-candidate hardening state for package entrypoint alignment, public type exports, package metadata, README/example references, dry-run package contents, dependency scope, and targeted validation results.

## Checks

- Confirmed the package entrypoint exports match the documented runtime API and explicit public type exports.
- Confirmed package metadata points to generated root entrypoint artifacts and does not expose internal subpaths.
- Confirmed README and Vite example references use public entrypoint imports and do not imply out-of-scope behavior.
- Confirmed the dry-run validation record reports expected package contents and no unintended source, test, workspace, example, or release-planning files.
- Confirmed no runtime dependencies, Next.js dependency, router integration, visual effects, animation timelines, camera presets, templates, or code generation were introduced.

## Result

No release-candidate bug was found during this triage. No source, public API, package metadata, dependency, or example code changes were required.

## Out-of-scope confirmation

No npm publish was performed. No git tag was created. No GitHub Release was created. This triage did not include v1.9.0 release baseline documentation or v1.9.0 release preparation work.
