# v1.9.0 Package Dry-Run Validation

This document records package dry-run validation for release-candidate confidence before any npm publish, git tag, GitHub Release, or milestone closeout action.

## Scope

Validation covered the package build output, dry-run publish contents, generated type declarations, package metadata, and publish-related contents for `packages/r3f-interactive-flow`.

No source behavior, public exports, package version, runtime dependencies, or peer dependency ranges were changed.

## Commands run

- `pnpm install --frozen-lockfile`
- `pnpm build`
- `pnpm --filter r3f-interactive-flow typecheck`
- `pnpm --filter r3f-interactive-flow pack:dry-run`
- `pnpm lint`
- `pnpm format`

## Package contents

`pnpm --filter r3f-interactive-flow pack:dry-run` rebuilt the package and reported these dry-run publish contents for `r3f-interactive-flow@1.8.0`:

- `CHANGELOG.md`
- `LICENSE`
- `README.md`
- `dist/index.cjs`
- `dist/index.cjs.map`
- `dist/index.d.cts`
- `dist/index.d.ts`
- `dist/index.js`
- `dist/index.js.map`
- `package.json`

The dry-run output did not include `src/`, tests, `tsup.config.ts`, workspace-only config, examples, release docs, roadmap docs, lockfiles, or shared AI chat/session/task links.

Generated type declarations are present as `dist/index.d.ts` and `dist/index.d.cts`. ESM and CJS entry files are present as `dist/index.js` and `dist/index.cjs`. Sourcemaps are included because the package build intentionally emits them.

The dry-run tarball summary reported a package size of 42.6 kB, an unpacked size of 225.2 kB, and 10 total files, which is reasonable for this small utility library with README, changelog, declarations, entry files, and sourcemaps.

## Metadata checks

Package metadata matches the dry-run contents:

- `main` points to the included CJS file, `./dist/index.cjs`.
- `module` points to the included ESM file, `./dist/index.js`.
- `types` points to the included declaration file, `./dist/index.d.ts`.
- `exports["."].types`, `exports["."].import`, and `exports["."].require` point to included files.
- `files` limits publish contents to `dist`, `README.md`, `LICENSE`, and `CHANGELOG.md`.
- `peerDependencies` remain `react`, `react-dom`, `three`, and `@react-three/fiber`.
- No `dependencies` field is present.
- `publishConfig.access` remains `public`.
- No Next.js dependency is present.
- No subpath exports are present.

## Out-of-scope confirmation

No npm publish was performed. No git tag was created. No GitHub Release was created. No milestone was closed.

No public API was expanded. No runtime dependencies were added. Next.js was not added as a dependency. No Next.js-specific router integration, visual effects, animation timeline system, camera presets, templates, CLI/codegen, router integration, or third-party animation wrappers were introduced.

## Result

Package dry-run validation passed. No package-output or metadata fix was required.
