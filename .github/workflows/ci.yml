name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - fix/ci-lockfile-and-workflow-cleanup

permissions:
  contents: write

jobs:
  update-lockfile:
    name: Update lockfile
    if: (github.head_ref == 'fix/ci-lockfile-and-workflow-cleanup' || github.ref == 'refs/heads/fix/ci-lockfile-and-workflow-cleanup') && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: fix/ci-lockfile-and-workflow-cleanup

      - name: Enable Corepack
        run: corepack enable

      - name: Setup pnpm
        run: corepack prepare pnpm@11.5.0 --activate

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies and update lockfile
        run: CI=false pnpm install

      - name: Commit lockfile
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pnpm-lock.yaml
          if git diff --cached --quiet; then
            echo "No lockfile changes"
            exit 0
          fi
          git commit -m "fix: update lockfile and clean up CI workflow"
          git push origin HEAD:fix/ci-lockfile-and-workflow-cleanup

  checks:
    name: Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup pnpm
        run: corepack prepare pnpm@11.5.0 --activate

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build library
        run: pnpm build

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Lint
        run: pnpm lint

      - name: Format check
        run: pnpm format

      - name: Build Vite example
        run: pnpm --filter vite-basic build
